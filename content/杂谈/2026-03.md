---
title: 2026年03月杂谈
date: 2026-03-01
tags:
  - 杂谈
  - 工作记录
---

## 2026-03-03

### 今日佳绩（小乐代写版）

> [!summary]
> 2026-03-03 的核心收获：把两条“会卡住交付”的链路（**日程/OAuth**、**飞书文档/权限**）都做成了可复用流程；同时把一次推送发布/代理问题也顺手排掉了。

- **✅ 日程落地（培训安排已入历）**
  - 已在 Google Calendar 创建日程：**「万家乐飞书aily和妙搭培训」**
  - 时间：**2026-03-05 16:00–17:00（Asia/Shanghai）**
  - 地点：**一号楼 201**
  - 价值：避免“口头通知 → 忘记/冲突”，后续还能直接把参会提醒、会议资料链接继续挂到同一个事件里。

- **✅ 飞书文档改稿交付（内容写回可验收）**
  - 目标文档：docx《解锁飞书新能力：aliy与妙搭快速上手》
  - 已完成写回：新版 **「初识妙搭」** 与 **「二、为什么用妙搭」** 两段
  - 验收点：文中出现 **“（小乐补充｜新版）”** 标记，说明替换已落地。
  - 价值：把“妙搭”这部分从“产品介绍碎片”升级成“可讲、可复用、可落地”的分享稿结构。

- **✅ 飞书权限补齐（超时根因解除）**
  - 关键 scope：`docx:document.block:convert` 已处于 **granted**
  - 影响：Docx 的 block/结构化内容读写不再出现“重试→最终超时”的假象。
  - 价值：后续再做类似文档自动化（替换章节、批量写回、结构化块处理）会稳定很多。

- **✅ 交付体验优化（输出更好读）**
  - Telegram 侧做了流式输出/分段策略调整，让长内容更“像人说话”而不是一坨墙。

- **✅ 部署/发布排障经验沉淀（可复用 SOP）**
  - 遇到“配置改了但没生效/重启没打到正在跑的 gateway”这类问题：优先确认真实监听进程，再做确定性重启。
  - Git 推送遇到本地代理端口（如 `127.0.0.1:7897`）导致连接断开：通常需要 **unproxy**，并检查 SSH 配置是否存在冲突条目。

### 技术记录（给未来的自己）

- **gogcli / OAuth（以后再来一次也不慌）**
  - 初始状态：本机已安装 gog（v0.11.0），但没有 token，需要走 OAuth。
  - 处理方式：在本机生成并保护 `client_secret.json`（配套 `.gitignore secrets/` + `chmod 600`），再完成远程授权步骤。
  - 产出：以后同类“帮同事/换机器/重装环境”的成本会明显降低。

- **飞书文档工具链（Docx）**
  - 结论：涉及 block/结构化内容的读写，权限不齐会表现为“重试→超时”。
  - 关键 scope：`docx:document.block:convert`

- **Quartz 博客发布（你说的 npx quartz 那套）**
  - Quartz 官方推荐的同步命令是：`npx quartz sync`（可带 `--no-pull`、`--message` 等）。
  - 你项目里也封装了更自动化的入口：`npm run publish:auto`（本质也是：build → commit → push，触发 GitHub Actions 部署）。

> [!info]
> 今日关键词：**交付闭环**（写回文档 + 权限补齐 + 同步发布） + **可复用排障套路**（代理/SSH/监听进程）。
